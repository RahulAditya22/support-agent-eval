# LLM provider configuration
# Set this locally; never commit a real API key.
LLM_API_KEY=

# Kaggle authentication for the local data-download step.
# Keep credentials out of the repository.
KAGGLE_API_TOKEN=
